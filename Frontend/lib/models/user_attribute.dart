class UserAttribute {
  // UserAuthInfo의 email 필드와 동일한 값
  String email;
  String name;
  String profileImage;

  UserAttribute(
      {required this.email,
      required this.name,
      required this.profileImage,
      });
}
